# Telegram transport
aiogram>=3.4,<4

# Configuration
python-dotenv>=1.0.0

# Redis: FSM + сессии при REDIS_URL (тысячи одновременных диалогов, несколько воркеров)
redis[hiredis]>=5.0.0

# Optional HTTP surface (same use cases as the bot)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Test tooling
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
