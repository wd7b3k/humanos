---
description: Все запросы в этом проекте — только про HumanOS
alwaysApply: true
---

# Область проекта: только HumanOS

- Репозиторий **HumanOS** (`/opt/humanos`). Считай, что пользователь пишет **только** про этот код и этот продукт.
- **Не** подставляй по умолчанию Text2voice, read-aloud, монорепо `text2voice` и другие проекты — только если пользователь **явно** назвал их в сообщении.
- Пути, правки, команды и анализ — относительно **корня этого репозитория** (HumanOS).
- Если запрос двусмысленен, уточни один короткий вопрос; по умолчанию трактуй в пользу HumanOS.
